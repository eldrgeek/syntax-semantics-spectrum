[build]
  publish = "public"
  functions = "netlify/functions"
  command = "echo 'static — no build step'"

[functions]
  node_bundler = "zisi"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
